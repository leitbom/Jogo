{
  "name": "tactical-shooter",
  "version": "2.0.0",
  "description": "Tactical Shooter Multiplayer — TypeScript + Hexagonal Architecture",
  "main": "dist/server/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "nodemon --watch src/server --ext ts --exec \"tsc -p tsconfig.json && node dist/server/main.js\"",
    "start": "node dist/server/main.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
